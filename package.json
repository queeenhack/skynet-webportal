{
  "private": true,
  "scripts": {
    "build:client": "yarn workspace skynet-client build",
    "start:client": "yarn workspace skynet-client start",
    "build:server": "yarn workspace siaviewnode-server build",
    "start:server": "yarn workspace siaviewnode-server start",
    "start": "concurrently \"yarn start:client\" \"yarn start:server\""
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {
    "concurrently": "5.1.0"
  }
}
